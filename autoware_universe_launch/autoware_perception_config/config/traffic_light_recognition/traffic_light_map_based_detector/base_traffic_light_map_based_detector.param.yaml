# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py perception` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/187982dbcc26348a8cd0f5fff1bc2d6b44a5c213/perception/autoware_traffic_light_map_based_detector/config/traffic_light_map_based_detector.param.yaml

/**:
  ros__parameters:
    max_vibration_pitch: 0.01745329251   # -0.5 ~ 0.5 deg in camera frame
    max_vibration_yaw: 0.01745329251     # -0.5 ~ 0.5 deg in camera frame
    max_vibration_height: 0.5            # -0.25 ~ 0.25 m in camera optical frame
    max_vibration_width: 0.5             # -0.25 ~ 0.25 m in camera optical frame
    max_vibration_depth: 0.5             # -0.25 ~ 0.25 m in camera optical frame
    max_detection_range: 200.0
    min_timestamp_offset: -0.3
    max_timestamp_offset: 0.0
    car_traffic_light_max_angle_range: 40.0
    pedestrian_traffic_light_max_angle_range: 80.0
