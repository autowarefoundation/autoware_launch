# autoware_sensing_launch

Shared sensing launch library. Sensor kits own the sensing entry point (`<sensor_model>_launch/launch/sensing.launch.xml`, included by `component_sensing.launch.xml`); this package hosts the reusable sensing units the kits build on.

## Package Dependencies

Please see `<exec_depend>` in `package.xml`.
