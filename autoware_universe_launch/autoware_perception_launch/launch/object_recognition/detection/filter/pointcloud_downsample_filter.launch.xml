<?xml version="1.0"?>
<launch>
  <!-- Map-less filter: approximate voxel downsample only -->
  <arg name="target_container" description="container to load the filter into"/>
  <arg name="use_intra_process" default="true"/>

  <arg name="input/pointcloud"/>
  <arg name="output/pointcloud"/>

  <arg name="downsample_voxel_size" default="0.1"/>

  <load_composable_node target="$(var target_container)">
    <composable_node pkg="autoware_pointcloud_preprocessor" plugin="autoware::pointcloud_preprocessor::ApproximateDownsampleFilterComponent" name="voxel_grid_downsample_filter">
      <remap from="input" to="$(var input/pointcloud)"/>
      <remap from="output" to="$(var output/pointcloud)"/>
      <param name="voxel_size_x" value="$(var downsample_voxel_size)"/>
      <param name="voxel_size_y" value="$(var downsample_voxel_size)"/>
      <param name="voxel_size_z" value="$(var downsample_voxel_size)"/>
      <extra_arg name="use_intra_process_comms" value="$(var use_intra_process)"/>
    </composable_node>
  </load_composable_node>
</launch>
