autoware_system_design_format: 0.3.0

name: LidarCenterpoint.module

instances:
  - name: lidar_centerpoint
    entity: LidarCenterpoint.node

subscribers:
  - name: pointcloud
publishers:
  - name: detected_objects

connections:
  - - subscriber.pointcloud
    - lidar_centerpoint.subscriber.pointcloud
  - - lidar_centerpoint.publisher.objects
    - publisher.detected_objects
