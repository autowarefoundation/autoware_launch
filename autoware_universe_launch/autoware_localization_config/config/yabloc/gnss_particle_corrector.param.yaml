# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py localization` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/98ef5348b6e9e22f9ca69c9f841655bd7fc44610/localization/yabloc/yabloc_particle_filter/config/gnss_particle_corrector.param.yaml

/**:
  ros__parameters:
    # Declared in AbstractParticleCorrector
    acceptable_max_delay: 1.0
    visualize: false

    mahalanobis_distance_threshold: 30.0 # If the distance to GNSS observation exceeds this, the correction is skipped.
    #
    for_fixed/max_weight: 5.0
    for_fixed/flat_radius: 0.5
    for_fixed/max_radius: 10.0
    for_fixed/min_weight: 0.5
    #
    for_not_fixed/flat_radius: 5.0
    for_not_fixed/max_radius: 20.0
    for_not_fixed/min_weight: 0.5
    for_not_fixed/max_weight: 1.0
