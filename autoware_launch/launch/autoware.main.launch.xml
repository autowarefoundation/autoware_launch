<?xml version="1.0" encoding="UTF-8"?>
<launch>
  <!-- Please refer to autoware.launch.xml for the parameters -->
  <!-- Essential parameters -->
  <arg name="map_path" description="point cloud and lanelet2 map directory path"/>
  <arg name="vehicle_model" default="sample_vehicle" description="vehicle model name"/>
  <arg name="sensor_model" default="sample_sensor_kit" description="sensor model name"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>
  <arg name="data_path" default="$(env HOME)/autoware_data" description="packages data and artifacts directory path"/>

  <!-- Map -->
  <arg name="lanelet2_map_file" default="lanelet2_map.osm" description="lanelet2 map file name"/>
  <arg name="pointcloud_map_file" default="pointcloud_map.pcd" description="pointcloud map file name"/>
  <!-- V2X -->
  <arg name="launch_v2x" default="true" description="launch V2X"/>
  <arg name="v2x_location" default="invalid_location" description="location for v2x (e.g. komatsu, shiojiri)"/>

  <!-- Redundant system parameters-->
  <arg name="is_redundant" default="false" description="is_redundant"/>
  <arg name="is_main_ecu" default="true" description="is_main_ecu"/>
  <arg name="is_multi_domain" default="false" description="is_multi_domain"/>

  <!-- Optional parameters for MRM test -->
  <!-- Planning-->
  <arg name="is_60kph" default="false" description="max speed limit is 60kph"/>

  <!-- Optional parameters for redundant system -->
  <!-- Control -->
  <let name="use_control_command_gate" value="true" if="$(var is_multi_domain)"/>
  <let name="use_control_command_gate" value="false" unless="$(var is_multi_domain)"/>

  <group scoped="false">
    <include file="$(find-pkg-share autoware_launch)/launch/autoware.launch.xml">
      <!-- Essential parameters -->
      <arg name="map_path" value="$(var map_path)"/>
      <arg name="vehicle_model" value="$(var vehicle_model)"/>
      <arg name="sensor_model" value="$(var sensor_model)"/>
      <arg name="pointcloud_container_name" value="$(var pointcloud_container_name)"/>
      <arg name="data_path" value="$(var data_path)"/>
      <!-- Map -->
      <arg name="lanelet2_map_file" value="$(var lanelet2_map_file)"/>
      <arg name="pointcloud_map_file" value="$(var pointcloud_map_file)"/>
      <!-- Redundant system parameters-->
      <arg name="is_redundant" value="$(var is_redundant)"/>
      <arg name="is_main_ecu" value="$(var is_main_ecu)"/>
      <arg name="is_multi_domain" value="$(var is_multi_domain)"/>
      <!-- Planning-->
      <arg name="is_60kph" value="$(var is_60kph)"/>
      <!-- Control -->
      <arg name="use_control_command_gate" value="$(var use_control_command_gate)"/>
      <!-- V2X -->
      <arg name="launch_v2x" value="$(var launch_v2x)"/>
      <arg name="v2x_location" value="$(var v2x_location)"/>
    </include>
  </group>
</launch>
