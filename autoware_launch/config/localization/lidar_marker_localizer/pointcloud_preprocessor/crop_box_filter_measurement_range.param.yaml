# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/1e08e0a159dd73673e5e06185bd9bb0c2090f6f9/sensing/autoware_pointcloud_preprocessor/config/crop_box_filter_node.param.yaml

/**:
  ros__parameters:
    min_x: -10.0 # {OVERRIDE}
    min_y: 0.0 # {OVERRIDE}
    min_z: -5.0 # {OVERRIDE}
    max_x: 10.0 # {OVERRIDE}
    max_y: 7.5 # {OVERRIDE}
    max_z: 5.0 # {OVERRIDE}
    negative: false
    processing_time_threshold_sec: 0.01
