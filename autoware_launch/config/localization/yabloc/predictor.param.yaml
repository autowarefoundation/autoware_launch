# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/98ef5348b6e9e22f9ca69c9f841655bd7fc44610/localization/yabloc/yabloc_particle_filter/config/predictor.param.yaml

/**:
  ros__parameters:
    visualize: true

    static_linear_covariance: 0.04
    static_angular_covariance: 0.006

    resampling_interval_seconds: 1.0
    num_of_particles: 500
    prediction_rate: 50.0

    cov_xx_yy: [2.0,0.25]
