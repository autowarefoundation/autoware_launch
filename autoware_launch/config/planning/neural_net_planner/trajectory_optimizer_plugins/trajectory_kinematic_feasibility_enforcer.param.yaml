# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/9b5a907f75581180d1ac2cd0c6f0fa3acba2d8cb/planning/autoware_trajectory_optimizer/config/plugins/trajectory_kinematic_feasibility_enforcer.param.yaml

/**:
  ros__parameters:
    # Plugin-specific parameters
    trajectory_kinematic_feasibility:
      # Maximum yaw rate [rad/s]. See kinematic_feasibility_enforcer.md for details
      max_yaw_rate_rad_s: 0.7
      # Time discretization
      time_step_s: 0.1  # Fixed time step for yaw rate calculations [s]
