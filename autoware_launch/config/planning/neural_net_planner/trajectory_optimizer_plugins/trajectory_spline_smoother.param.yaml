# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/a82889caf3b1154fde6e8327c4836466b8ed0960/planning/autoware_trajectory_optimizer/config/plugins/trajectory_spline_smoother.param.yaml

/**:
  ros__parameters:
    trajectory_spline_smoother:
      # Spline interpolation
      interpolation_resolution_m: 0.2  # [m] Interpolation resolution for Akima spline
      max_distance_discrepancy_m: 5.0  # [m] Maximum position deviation allowed for orientation copying
      preserve_input_trajectory_orientation: false  # Flag to indicate if orientation from original trajectory should be copied
