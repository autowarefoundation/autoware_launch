# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py planning` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/0a309acc11555591ed7844d36b5c0b9818abe225/planning/autoware_trajectory_optimizer/config/plugins/trajectory_point_fixer.param.yaml

/**:
  ros__parameters:
    trajectory_point_fixer:
      # Distance thresholds for point processing
      remove_close_points: true    # Enable removal of close proximity points
      resample_close_points: true     # Enable resampling of close proximity points
      min_dist_to_remove_m: 0.01  # [m] Minimum distance to remove close proximity points
      min_dist_to_resample_m: 0.05   # [m] Minimum distance to merge close proximity points
      stop_detection_velocity_threshold_mps: 0.3  # [m/s] Velocity threshold for fallback stop detection in slow clusters
