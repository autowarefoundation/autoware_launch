# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py planning` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/b681c74fa856ac256a9d327795a988f1e883c5bc/planning/behavior_velocity_planner/autoware_behavior_velocity_detection_area_module/config/detection_area.param.yaml

/**:
  ros__parameters:
    detection_area:
      stop_margin: 0.0
      target_filtering:
        pointcloud: true
        unknown: false
        car: false
        truck: false
        bus: false
        trailer: false
        motorcycle: false
        bicycle: false
        pedestrian: false
        animal: false
        hazard: false
        over_drivable: false
        under_drivable: false
      use_dead_line: false
      dead_line_margin: 5.0
      unstoppable_policy: "stop_after_stopline" # "go" or "force_stop" or "stop_after_stopline"
      max_deceleration: 2.5 # {OVERRIDE}
      delay_response_time: 0.5
      state_clear_time: 2.0
      hold_stop_margin_distance: 0.0
      distance_to_judge_over_stop_line: 0.5
      enable_rtc: false # If set to true, the scene modules require approval from the rtc (request to cooperate) function. If set to false, the modules can be executed without requiring rtc approval
      suppress_pass_judge_when_stopping: false
      enable_detected_obstacle_logging: false # Enable/disable detected obstacle logging
