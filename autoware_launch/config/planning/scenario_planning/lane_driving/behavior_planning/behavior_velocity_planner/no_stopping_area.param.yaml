# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py planning` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/dba91b0563ee67e5119c837280a28b2e9a518633/planning/behavior_velocity_planner/autoware_behavior_velocity_no_stopping_area_module/config/no_stopping_area.param.yaml

/**:
  ros__parameters:
    no_stopping_area:
      state_clear_time: 2.0           # [s] time to clear stop state
      stuck_vehicle_vel_thr: 0.833    # 0.833m/s = 3.0km/h
      stop_margin: 0.0                # [m] margin to stop line at no stopping area
      dead_line_margin: 1.0           # [m] dead line offset go at no stopping area
      stop_line_margin: 1.0           # [m] margin to auto-gen stop line at no stopping area
      detection_area_length: 200.0    # [m] used to create detection area polygon
      stuck_vehicle_front_margin: 6.0 # [m] obstacle stop max distance(5.0m)
      enable_rtc: false # If set to true, the scene modules require approval from the rtc (request to cooperate) function. If set to false, the modules can be executed without requiring rtc approval
