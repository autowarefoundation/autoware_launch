# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/e0002f3e36defd695a026df741b5761df615d7e1/planning/autoware_costmap_generator/config/costmap_generator.param.yaml

/**:
  ros__parameters:
    costmap_frame: "map"
    vehicle_frame: "base_link"
    map_frame: "map"
    update_rate: 10.0
    activate_by_scenario: false
    grid_min_value: 0.0
    grid_max_value: 1.0
    grid_resolution: 0.3
    grid_length_x: 70.0
    grid_length_y: 70.0
    grid_position_x: 0.0
    grid_position_y: 0.0
    maximum_lidar_height_thres: 2.5  # [m] when use_points is true, ignore points with a z value above this (in the vehicle_frame)
    minimum_lidar_height_thres: 0.0  # [m] when use_points is true, ignore points with a z value bellow this (in the vehicle_frame)
    use_wayarea: true
    use_parkinglot: true
    use_objects: true
    use_points: true
    expand_polygon_size: 0.5
    size_of_expansion_kernel: 9
