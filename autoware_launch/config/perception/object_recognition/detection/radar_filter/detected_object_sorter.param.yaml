# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py perception` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/c3a838767a41e432baf0cd77aeee542cfb276ab3/perception/autoware_object_sorter/config/detected_object_sorter.param.yaml

/**:
  ros__parameters:
    range_calc_frame_id: "base_link"
    range_calc_offset:
      x: 0.0 # [m]
      y: 0.0 # [m]

    range_thresholding_mode: "distance" # [distance, area]

    sort_target:
      UNKNOWN:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      CAR:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      TRUCK:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      BUS:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      TRAILER:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      MOTORCYCLE:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      BICYCLE:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
         # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      PEDESTRIAN:
        publish: true
        min_velocity_threshold: 5.5 # {OVERRIDE} [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 1000.0 # {OVERRIDE} [m]
          min_distance: 80.0 # {OVERRIDE} [m]
          # when range_thresholding_mode = area
          max_x: 1000.0 # {OVERRIDE} [m]
          min_x: -1000.0 # {OVERRIDE} [m]
          max_y: 1000.0 # {OVERRIDE} [m]
          min_y: -1000.0 # {OVERRIDE} [m]

      ANIMAL:
        publish: true
        min_velocity_threshold: 0.0 # [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 100.0 # [m]
          min_distance: 30.0 # [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]

      HAZARD:
        publish: true
        min_velocity_threshold: 0.0 # [m/s]
        range_threshold:
          # when range_thresholding_mode = distance
          max_distance: 100.0 # [m]
          min_distance: 30.0 # [m]
          # when range_thresholding_mode = area
          max_x: 100.0 # [m]
          min_x: -100.0 # [m]
          max_y: 100.0 # [m]
          min_y: -100.0 # [m]
