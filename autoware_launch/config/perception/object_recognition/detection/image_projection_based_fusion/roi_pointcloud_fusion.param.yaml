# This file is managed by workflow.
# Keys marked with '*OVERRIDE*' are persisted as variant overrides.
# If you want to modify a field, make sure that the field has comment including -> # *OVERRIDE*
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/bbe8062f7ac4a520a20034ff0f35cc9e11e96a06/perception/autoware_image_projection_based_fusion/config/roi_pointcloud_fusion.param.yaml
# Reference in this repository: .config-sync/perception/autoware_image_projection_based_fusion/config/roi_pointcloud_fusion.param.yaml

/**:
  ros__parameters:
    min_cluster_size: 2
    max_cluster_size: 20
    cluster_2d_tolerance: 0.5
    roi_scale_factor: 1.0
    max_object_size: 2.0
    override_class_with_unknown: false
    fusion_enabled_classes:
      UNKNOWN: true
      CAR: false
      BUS: false
      TRUCK: false
      TRAILER: false
      MOTORCYCLE: false
      BICYCLE: true
      PEDESTRIAN: true
