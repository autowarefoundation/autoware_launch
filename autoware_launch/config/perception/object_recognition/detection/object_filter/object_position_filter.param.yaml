# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/a595b365d4e4fa7184f6d104d6ef1802285f6bbb/perception/autoware_detected_object_validation/config/object_position_filter.param.yaml

/**:
  ros__parameters:
    filter_target_label:
      UNKNOWN : true
      CAR : false
      TRUCK : false
      BUS : false
      TRAILER : false
      MOTORCYCLE : false
      BICYCLE : false
      PEDESTRIAN : false

    upper_bound_x: 100.0
    lower_bound_x: 0.0
    upper_bound_y: 10.0
    lower_bound_y: -10.0
