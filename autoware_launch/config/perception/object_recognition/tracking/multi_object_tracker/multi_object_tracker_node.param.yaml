# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py perception` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/3629ec2e44e84aceb3584dad8c248f7c280c38da/perception/autoware_multi_object_tracker/config/multi_object_tracker_node.param.yaml

/**:
  ros__parameters:
    # default tracker node parameters
    world_frame_id: map
    ego_frame_id: base_link

    # publish-trigger
    publish_on_timer: true # {OVERRIDE}
    publish_rate: 10.0

    # ego pose source: "tf" (look up from TF tree) or "odometry" (interpolate from ~/input/odometry)
    ego_source: tf
    # delay compensation, least -> most:
    # "none" | "publish_delay" (detection + update->publish delay) | "odometry" (up to latest odometry) | "full" (to now)
    delay_compensation: full # {OVERRIDE}

    consider_odometry_uncertainty: false

    # Per-tracker-type configuration
    tracker_configs:
      polygon_tracker:
        enable_velocity_estimation: true
        enable_motion_output:
          unknown:    false
          car:        true
          truck:      true
          bus:        true
          trailer:    true
          motorcycle: true
          bicycle:    true
          pedestrian: true
          animal:     true
          hazard:     false
      static_tracker:
        convert_polygon_to_bbox: false # convert POLYGON shape to BOUNDING_BOX at publish time

    # pruning parameters: tracker-pair redundancy thresholds per label-pair class
    pruning_pedestrian_pair_min_iou: 0.1  # [ratio] 1D IoU between two pedestrians
    pruning_known_pair_min_iou: 0.1       # [ratio] 2D IoU between two known-class objects
    pruning_unknown_pair_min_giou: 0.1    # [ratio] GIoU between two unknowns; negative admits disjoint pairs
    pruning_unknown_pair_max_gap: 1.0     # [m] boundary-gap bound for disjoint unknown-unknown pairs

    # debug parameters
    publish_processing_time: true # {OVERRIDE}
    publish_processing_time_detail: false
    publish_tentative_objects: false
    publish_debug_markers: false
    diagnostics_warn_delay: 0.5      # [sec]
    diagnostics_error_delay: 1.0     # [sec]
    diagnostics_warn_extrapolation: 0.5      # [sec]
    diagnostics_error_extrapolation: 1.0      # [sec]
