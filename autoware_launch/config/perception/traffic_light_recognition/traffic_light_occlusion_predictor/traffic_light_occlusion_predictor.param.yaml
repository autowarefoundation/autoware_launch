# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py perception` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/f808124ca8f7b3370415577c2e6b49ebc3048279/perception/autoware_traffic_light_occlusion_predictor/config/traffic_light_occlusion_predictor.param.yaml

/**:
  ros__parameters:
    azimuth_occlusion_resolution_deg: 0.15 # degree
    elevation_occlusion_resolution_deg: 0.08  # degree
    max_valid_pt_dist: 50.0
    # the node would wait at most max_wait_t seconds for cloud whose timestamp different with the image is less than max_image_cloud_delay.
    # If no cloud with timestamp difference smaller than max_image_cloud_delay is found, the node would publish occlusion ratio of all
    # traffic lights as 0
    max_image_cloud_delay: 0.5
    max_wait_t: 0.05
    max_occlusion_ratio: 50
