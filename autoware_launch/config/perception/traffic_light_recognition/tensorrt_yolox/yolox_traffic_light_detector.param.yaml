# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py perception` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/f885848d174ef5f56edf48439c51629de81f86f9/perception/autoware_tensorrt_yolox/config/yolox_traffic_light_detector.param.yaml

/**:
  ros__parameters:
    dla_core_id: -1 # If positive ID value is specified, the node assign inference task to the DLA core.
    gpu_id: 0 # GPU ID to select CUDA Device
    calibration_image_list_path: "" # Path to a file which contains path to images. Those images will be used for int8 quantization.

    # default params of tensorrt_yolox package, only effective for semantic segmentation model
    is_roi_overlap_segmentation: false
    overlap_roi_score_threshold: 0.3
    is_publish_color_mask: false
    roi_overlay_segmentation_label:
      UNKNOWN : false
      CAR : false
      TRUCK : false
      BUS : false
      MOTORCYCLE : false
      BICYCLE : false
      PEDESTRIAN : false
      ANIMAL: false
      HAZARD: false
