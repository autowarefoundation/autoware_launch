# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py system` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/ff57ebf8e154b3662406e1df1224ca9146430e39/system/autoware_command_mode_switcher/config/default.param.yaml

/**:
  ros__parameters:
    update_rate: 10.0
    multiple_ecu_enabled: false
    plugins:
      - autoware::command_mode_switcher::StopSwitcher
      - autoware::command_mode_switcher::AutonomousSwitcher
      - autoware::command_mode_switcher::LocalSwitcher
      - autoware::command_mode_switcher::RemoteSwitcher
      - autoware::command_mode_switcher::EmergencyStopSwitcher
      - autoware::command_mode_switcher::ComfortableStopSwitcher
      - autoware::command_mode_switcher::PullOverSwitcher
    plugin_parameters:
      # <plugin name>:  e.g. autoware::command_mode_switcher::StopSwitcher:
      #   <parameter name>: <value>
      autoware::command_mode_switcher::ComfortableStopSwitcher:
        hazard_lights_hz: 20
        min_acceleration: -1.0
        max_jerk: 0.6
        min_jerk: -0.6
