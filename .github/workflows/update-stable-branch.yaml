name: Update Stable Branch

on:
  push:
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  workflow_dispatch:

jobs:
  update-stable:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate token
        id: generate-token
        uses: tibdex/github-app-token@v2
        with:
          app_id: ${{ secrets.APP_ID }}
          private_key: ${{ secrets.PRIVATE_KEY }}

      - name: Set git config
        uses: autowarefoundation/autoware-github-actions/set-git-config@v1
        with:
          token: ${{ steps.generate-token.outputs.token }}

      - name: Determine target tag
        id: determine-tag
        run: |
          # Get all tags matching vX.Y.Z pattern and sort them
          LATEST_TAG=$(git tag -l 'v[0-9]*.[0-9]*.[0-9]*' | sort -V | tail -n 1)
          echo "Latest tag: $LATEST_TAG"

          if [ -z "$LATEST_TAG" ]; then
            echo "Error: No matching tags found."
            exit 1
          fi

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Workflow manually triggered. Targeting latest tag: $LATEST_TAG"
            TARGET_TAG="$LATEST_TAG"
          else
            CURRENT_TAG="${GITHUB_REF#refs/tags/}"
            echo "Current tag: $CURRENT_TAG"

            if [ "$CURRENT_TAG" != "$LATEST_TAG" ]; then
              echo "Error: $CURRENT_TAG is not the latest vX.Y.Z tag. Latest is $LATEST_TAG"
              exit 1
            fi

            echo "$CURRENT_TAG is the latest vX.Y.Z tag. Updating stable branch."
            TARGET_TAG="$CURRENT_TAG"
          fi

          echo "target_tag=$TARGET_TAG" >> $GITHUB_OUTPUT

      - name: Update stable branch
        run: |
          TARGET_TAG="${{ steps.determine-tag.outputs.target_tag }}"
          git checkout "$TARGET_TAG"
          git checkout -B stable
          git push origin stable --force-with-lease

      - name: Run sync-branches
        uses: autowarefoundation/autoware-github-actions/sync-branches@v1
        with:
          token: ${{ steps.generate-token.outputs.token }}
          base-branch: tier4/main
          sync-pr-branch: stable-to-tier4-main-sync
          sync-target-repository: ${{ github.server_url }}/${{ github.repository }}.git
          sync-target-branch: stable
          pr-title: "chore: sync branch stable (${{ steps.determine-tag.outputs.target_tag }}) with tier4/main"
          pr-reviewers: rej55 # hard-coded for now; might want to assign a team instead of individuals in the future
          pr-labels: |
            bot
            sync-latest-stable
          auto-merge-method: merge
