name: launch-plus resolve check

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: 0 6 * * * # daily 6 AM UTC
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

env:
  LAUNCH_PLUS_VERSION: v0.1.1

jobs:
  resolve:
    name: Resolve (dry-run build)
    runs-on: ubuntu-24.04
    container: ros:jazzy
    timeout-minutes: 30
    steps:
      - name: Install system dependencies
        run: |
          apt-get update && apt-get install -y --no-install-recommends \
            curl \
            git \
            python3-pip \
            python3-yaml \
            ros-dev-tools

      - name: Initialize rosdep
        run: |
          rosdep init 2>/dev/null || true
          rosdep update
        shell: bash

      - name: Install launch-plus
        run: |
          curl -fsSL "https://github.com/paulsohn/launch-plus/releases/download/${LAUNCH_PLUS_VERSION}/launch-plus-linux-amd64-ubuntu24.04" \
            -o /usr/local/bin/launch-plus
          chmod +x /usr/local/bin/launch-plus
          launch-plus --version

      # Checkout ci_depends.repos and ML param assets from this repo.
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            ci_depends.repos
            .github/assets/autoware_data
          sparse-checkout-cone-mode: false
          path: checkout

      - name: Install Autoware ML param files
        run: cp -r checkout/.github/assets/autoware_data "$HOME/autoware_data"

      # For PRs (including from forks): rewrite the autoware_launch entry
      # to point at the PR head repo/branch instead of main.
      - name: Patch ci_depends.repos for PR
        if: github.event_name == 'pull_request'
        shell: python3 {0}
        run: |
          import yaml

          with open("checkout/ci_depends.repos") as f:
              data = yaml.safe_load(f)

          entry = data["repositories"]["launcher/autoware_launch"]
          entry["url"] = "${{ github.event.pull_request.head.repo.clone_url }}"
          entry["version"] = "${{ github.event.pull_request.head.ref }}"

          with open("checkout/ci_depends.repos", "w") as f:
              yaml.dump(data, f, default_flow_style=False)

      - name: Prepare workspace
        run: |
          mkdir -p ws
          cp checkout/ci_depends.repos ws/ci_depends.repos

      - name: Index
        working-directory: ws
        run: launch-plus index ci_depends.repos

      - name: Resolve (dry-run build)
        working-directory: ws
        shell: bash
        run: |
          source /opt/ros/jazzy/setup.bash
          launch-plus build \
            autoware_launch autoware.launch.xml \
            sensor_model:=sample_sensor_kit \
            vehicle_model:=sample_vehicle \
            "map_path:=[map_path]" \
            --allow-global-arg-cascade \
            --apply-launch-arg-defaults \
            --apply-opaque-file-access \
            --rosdep \
            --dry-run
