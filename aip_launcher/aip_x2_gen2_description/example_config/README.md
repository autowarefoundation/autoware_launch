# Example Configuration for Description

This is an example configuration for this sensor kit description.

If you encounter a launcher issue with this package, the launcher may be searching for a `config` directory.
Please provide the appropriate `config_dir` arguments (e.g., pointing individual parameters) rather than copying or renaming this directory to `config`.
