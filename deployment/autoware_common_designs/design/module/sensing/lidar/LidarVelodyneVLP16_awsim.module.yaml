autoware_system_design_format: 0.1.0

name: LidarVelodyneVLP16_awsim.module

base: LidarVelodyneVLP16.module

remove:
  instances:
    - name: lidar

override:
  external_interfaces:
    input:
      - name: pointcloud_raw_ex

  connections:
    - from: input.pointcloud_raw_ex
      to: crop_box_filter_self.input.input
