autoware_system_design_format: v0.1.0

name: HesaiLidar_decode.module

base: HesaiLidar.module

override:
  instances:
  - name: lidar
    entity: NebulaHesaiPandar128E4X_decode.node

  external_interfaces:
    input:
    - name: pandar_packets

  connections:
  - from: input.pandar_packets
    to: lidar.input.pandar_packets
